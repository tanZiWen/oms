package com.prosnav.oms.util;

import java.util.Timer;

import javax.servlet.http.HttpServlet;

public class poolTimerservlet extends HttpServlet {
	Timer timer = null;
	public void init(){
        //System.out.println(uid.getStringId());

//		Calendar date = Calendar.getInstance();
//		date.set(date.get(Calendar.YEAR), date.get(Calendar.MONTH), date.get(Calendar.DATE), 1, 0, 0);
//		timer = new Timer();
//		timer.schedule(new PoolMailTimer(), date.getTime(), 24*60*60*1000); 
	}
	
	 
	        
	  
}
